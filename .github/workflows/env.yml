name: containers
on: push
jobs:
 node-docker:
   runs-on: ubuntu-latest
   container:
     image: docker.io/nginx
   steps:
     - name: Log the node version
       run: |
            hostname
            cat /etc/os-release 

